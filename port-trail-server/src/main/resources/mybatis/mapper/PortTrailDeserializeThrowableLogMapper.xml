<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.addenda.porttrail.server.mapper.PortTrailDeserializeThrowableLogMapper">
  <resultMap type="cn.addenda.porttrail.server.entity.PortTrailDeserializeThrowableLog"
             id="PortTrailDeserializeThrowableLogMap">
    <id property="id" column="id" jdbcType="BIGINT"/>
  </resultMap>

  <sql id="allColumn">
    `id`
    , `deserialize_type`, `bytes`, `throwable_stack`
  </sql>

  <sql id="setCondition">
    <if test="deserializeType != null">
      `deserialize_type` = #{deserializeType,jdbcType=VARCHAR},
    </if>
    <if test="bytes != null">
      `bytes` = #{bytes},
    </if>
    <if test="throwableStack != null">
      `throwable_stack` = #{throwableStack,jdbcType=VARCHAR},
    </if>
  </sql>

  <sql id="queryCondition">
    <if test="id != null">
      and `id` = #{id,jdbcType=BIGINT}
    </if>
    <if test="deserializeType != null">
      and `deserialize_type` = #{deserializeType,jdbcType=VARCHAR}
    </if>
    <if test="bytes != null">
      and `bytes` = #{bytes}
    </if>
    <if test="throwableStack != null">
      and `throwable_stack` = #{throwableStack,jdbcType=VARCHAR}
    </if>
  </sql>

  <insert id="insert" keyProperty="id" useGeneratedKeys="true">
    insert into t_port_trail_deserialize_throwable_log
    <trim prefix="set" suffixOverrides=",">
      id = #{id,jdbcType=BIGINT},
      <include refid="setCondition"/>
    </trim>
  </insert>

  <update id="updateById">
    update t_port_trail_deserialize_throwable_log
    <trim prefix="set" suffixOverrides=",">
      <include refid="setCondition"/>
    </trim>
    where id = #{id,jdbcType=BIGINT}
    and delete_flag = 'N'
  </update>

  <delete id="deleteById">
    update t_port_trail_deserialize_throwable_log
    set delete_flag = 'Y',
        delete_dt   = now(3)
    where id = #{id,jdbcType=BIGINT}
      and delete_flag = 'N'
  </delete>

  <delete id="deleteByEntity">
    update t_port_trail_deserialize_throwable_log set delete_flag = 'Y', delete_dt = now(3)
    <trim prefix="where" prefixOverrides="and">
      <include refid="queryCondition"/>
      and delete_flag = 'N'
    </trim>
  </delete>

  <select id="queryByEntity" resultMap="PortTrailDeserializeThrowableLogMap">
    select
    <include refid="allColumn"/>
    from t_port_trail_deserialize_throwable_log
    <trim prefix="where" prefixOverrides="and">
      <include refid="queryCondition"/>
      and delete_flag = 'N'
    </trim>
  </select>

  <select id="queryById" resultMap="PortTrailDeserializeThrowableLogMap">
    select
    <include refid="allColumn"/>
    from t_port_trail_deserialize_throwable_log
    <trim prefix="where" prefixOverrides="and">
      id = #{id,jdbcType=BIGINT}
      and delete_flag = 'N'
    </trim>
  </select>

  <select id="queryByIdList" resultMap="PortTrailDeserializeThrowableLogMap">
    select
    <include refid="allColumn"/>
    from t_port_trail_deserialize_throwable_log
    <trim prefix="where" prefixOverrides="and">
      id in
      <foreach collection="idList" open="(" close=")" item="id" separator=",">
        #{id,jdbcType=BIGINT}
      </foreach>
      and delete_flag = 'N'
    </trim>
  </select>

  <select id="countByEntity" resultType="java.lang.Long">
    select count(*) from t_port_trail_deserialize_throwable_log
    <trim prefix="where" prefixOverrides="and">
      <include refid="queryCondition"/>
      and delete_flag = 'N'
    </trim>
  </select>
</mapper>


